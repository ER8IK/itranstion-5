# Quick Start Guide - 5 минут до запуска! 🚀

## Вариант 1: Локальная разработка (Рекомендуется для изучения)

### Шаг 1: Установка зависимостей (1 мин)
```bash
# Убедитесь что установлены:
# - Node.js 18+ (node --version)
# - PostgreSQL 14+ (psql --version)

# Клонируйте репозиторий
cd user-management-app
```

### Шаг 2: База данных (1 мин)
```bash
# Создайте базу данных
createdb user_management

# Если нужно создать пользователя:
psql postgres
CREATE USER myuser WITH PASSWORD 'mypassword';
ALTER USER myuser CREATEDB;
\q
```

### Шаг 3: Backend (2 мин)
```bash
cd backend

# Установите зависимости
npm install

# Настройте окружение
cp .env.example .env

# Отредактируйте .env (минимальные настройки):
# DB_USER=ваш_пользователь
# DB_PASSWORD=ваш_пароль
# DB_NAME=user_management
# JWT_SECRET=любая_длинная_строка
# EMAIL_USER=ваш_gmail@gmail.com (опционально)
# EMAIL_PASSWORD=ваш_app_password (опционально)

# Создайте таблицы и индексы
npm run migrate

# Запустите сервер
npm run dev
```

✅ Backend работает на http://localhost:5000

### Шаг 4: Frontend (1 мин)
```bash
cd ../frontend

# Установите зависимости
npm install

# Запустите приложение
npm run dev
```

✅ Frontend работает на http://localhost:3000

### Шаг 5: Первый вход
1. Откройте http://localhost:3000
2. Войдите с тестовым аккаунтом:
   - **Email**: `admin@example.com`
   - **Password**: `admin`
3. Готово! Вы в админ панели 🎉

---

## Вариант 2: Docker (Самый быстрый способ!)

### Одна команда для всего! ⚡

```bash
# 1. Запустите все сервисы (PostgreSQL + Backend + Frontend)
docker-compose up -d

# 2. Создайте таблицы (только первый раз)
docker-compose exec backend npm run migrate

# 3. Откройте браузер
# http://localhost:3000
```

**Логин**: admin@example.com / admin

**Готово!** Всё работает! 🎊

### Полезные команды Docker:
```bash
# Посмотреть логи
docker-compose logs -f

# Перезапустить
docker-compose restart

# Остановить
docker-compose down

# Пересобрать после изменений
docker-compose up -d --build
```

---

## ⚙️ Настройка Email (Опционально)

Для отправки email верификации нужно настроить SMTP.

### Gmail (Самый простой способ):

1. **Включите 2FA** в Google аккаунте
2. **Создайте App Password**:
   - Google Account → Security → 2-Step Verification
   - App passwords → Generate
3. **Обновите .env**:
```env
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=ваш_email@gmail.com
EMAIL_PASSWORD=сгенерированный_app_password
EMAIL_FROM=noreply@yourapp.com
```

4. **Перезапустите backend**

✅ Теперь при регистрации будут отправляться email!

---

## 🎮 Что попробовать:

### 1. Регистрация нового пользователя
- Кликните "Register here"
- Введите любое имя, email и пароль (даже 1 символ!)
- Получите мгновенное подтверждение

### 2. Email верификация
- Проверьте почту (может быть в спаме)
- Кликните ссылку для активации
- Статус изменится на "active"

### 3. Управление пользователями
- **Выберите** пользователей чекбоксами
- **Заблокируйте** (Block button)
- Попробуйте войти заблокированным - не получится!
- **Разблокируйте** (🔓 иконка)
- **Удалите** (🗑️ иконка) - РЕАЛЬНО удаляется из БД!

### 4. Тест уникального индекса
- Попробуйте зарегистрироваться с существующим email
- Получите ошибку от базы данных (не от кода!)

### 5. Самоудаление
- Выберите себя в таблице
- Нажмите Delete
- Вас автоматически разлогинит!

---

## 🐛 Проблемы?

### Backend не запускается
```bash
# Проверьте PostgreSQL
pg_isready

# Проверьте что БД создана
psql -l | grep user_management

# Проверьте логи
cd backend
npm run dev
```

### Frontend показывает ошибки
```bash
# Убедитесь что backend запущен
curl http://localhost:5000/api/health

# Проверьте консоль браузера (F12)
```

### Email не отправляются
- Это нормально! Email опционален
- Пользователи могут работать без верификации
- Если нужно - настройте Gmail App Password выше

### Forgot Password
```bash
# Сбросьте пароль через БД
psql user_management
UPDATE users SET password = '$2b$10$...' WHERE email = 'user@example.com';
```

Или просто создайте нового пользователя через регистрацию!

---

## 📚 Следующие шаги:

1. **Изучите код**:
   - `backend/src/migrate.ts` - посмотрите на UNIQUE INDEX
   - `backend/src/middleware/auth.ts` - проверка пользователя
   - `frontend/src/components/Dashboard.tsx` - таблица и toolbar

2. **Прочитайте документацию**:
   - `README.md` - полная документация
   - `DEPLOYMENT.md` - деплой в продакшн
   - `REQUIREMENTS_CHECKLIST.md` - все требования

3. **Задеплойте**:
   - Heroku (бесплатно)
   - Railway (бесплатно)
   - Render (бесплатно)
   - DigitalOcean, AWS, etc.

---

## 🎯 Ключевые особенности проекта:

✅ **UNIQUE INDEX** на email в PostgreSQL - гарантия уникальности  
✅ **Bootstrap UI** - профессиональный, бизнес-вид  
✅ **JWT аутентификация** - безопасно  
✅ **Async email** - не блокирует регистрацию  
✅ **Multiple selection** - чекбоксы + toolbar  
✅ **Real deletion** - не маркировка, а DELETE  
✅ **Self-management** - можно удалить себя  
✅ **Production ready** - Docker, TypeScript, валидация  

---

**Удачи! Если что-то не работает - смотрите README.md или DEPLOYMENT.md** 🚀
